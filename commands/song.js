const Discord = require('discord.js');

const client = new Discord.Client();

module.exports = {
    name: 'song',
    description: 'Play songs through the bot!',
    execute(message, args) {
        //
        
    },
};